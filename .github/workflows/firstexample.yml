name: My First workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "hello world"
        - name: list files
          run: ls -al
        - name: read file
          run: cat README.md 

